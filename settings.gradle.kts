pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "BankUtilsMultiplatform"
include(":androidApp")
include(":library:country")
